# Задание 1: Уязвимость аутентификации 
1-B 
2-C 
3-A
## Задание 2: Уязвимость аутентификации ##
1.Какой тип уязвимости (по OWASP) присутствует в коде? Приведите точное название.
Тип уязвимости – инъекций SQL-инъекция
2.Как злоумышленник может её использовать? Приведите конкретный пример вредоносных входных данных для параметра username
В параметр username злоумышленник может передать вредоносный код и зайти от администратора
admin' – 
этот код вернёт пароль администратора, и он получит доступ к данным
3.Напишите безопасную версию этого фрагмента кода, используя параметризованный запрос.
def check_password(username, input_password):

    query = "SELECT password FROM users WHERE username =?"
    
    user = db.query(query, (username,))
    
    if user and user['password'] == input_password:
        return True
    return False
С “?” база данных будет читать sql запрос не как код, а как обычную строку после чего просто не найдёт его в базе данных и вернёт ошибку
## Задание 3: Уязвимость обработки данных ##
Вопросы для анализа:

1.Какая опасная уязвимость здесь присутствует? К чему она может привести?Опишите шаги, как злоумышленник может использовать эту уязвимость для выполнения произвольного кода на сервере.
Неограниченная загрузка файлов (Unrestricted File Upload) – злоумышленник может удалённо выполнить произвольный код и сможет контролировать БД
2.Опишите шаги, как злоумышленник может использовать эту уязвимость для выполнения произвольного кода на сервере. Что он должен загрузить вместо картинки?
Злоумышленник вместо картинки загружает вредоносный код с
Взломщик создаёт скрипт с кодом и отправляет этот файл на сервер как картинку для профиля /uploads/users/shell.php этот код выполняет сервер после чего взломщик, зайдя на сайт видит все данные
3. Предложите 3 конкретных меры для защиты этой функции (например, проверка расширения, переименование файла, проверка MIME-типа). 
1.Проверка расширения файла запрещая всё кроме разрешенных форматов
2. Проверка MIME-типа и содержимого проверять finfo_file () (реальный MIME), а не только $_FILES['type']
3.Запретить папке выполнять какие-либо скрипты и программы, которые будут в неё загружены
## Задание 4: Уязвимость управления сессией ##
Вопросы для анализа:
1.Какие две разные уязвимости (или антипаттерна безопасности) вы видите в этом коде? Дайте названия.
	1. Insecure Direct Object Reference - Небезопасная прямая ссылка. Не проверяет права доступа 
	2. Хранение чувствительных данных в localStorage (Sensitive Data Exposure) настройки хранятся на клиенте в открытом виде, доступны любому JS-коду
2. Как злоумышленник может эскалировать привилегии (получить доступ к данным другого пользователя), используя эти уязвимости? Опишите пошаговый сценарий.
1. Хакер через консоль в браузере видит код приложения и берёт userId 
2. Заменяет командой айди на тот который скопировал
3. После обновления страницы вызывается функция 
4. Сервер не проверяя даёт доступ к пользователю, который нужен хакеру (например администратор)
4. Предложите безопасную архитектуру для хранения и передачи пользовательских настроек и идентификатора. Куда стоит перенести логику и как безопасно передавать user_id?
Стоит перенести логику на бэкэнд, потому что опасно доверять серверу получать id как запрос
Хранить только безопасные настройки на клиенте 
Игнорировать userId и брать его из сессии



